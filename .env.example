# Optional docker compose settings. Copy to .env at the project root.
# Change these ports if the earlier local preview is still using 3000/8000.
FRONTEND_PORT=3000
BACKEND_PORT=8000
DJANGO_SECRET_KEY=change-this-long-random-secret-before-sharing
